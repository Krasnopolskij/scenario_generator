# Подключение к Neo4j
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=changeme
NEO4J_DATABASE=neo4j

# Источники данных
# ATT&CK STIX JSON (через запятую)
ATTACK_JSON_URLS=https://github.com/mitre-attack/attack-stix-data/raw/refs/heads/master/enterprise-attack/enterprise-attack.json

# CWE: ссылки на CSV-архивы (через запятую)
CWE_CSV_URLS=https://cwe.mitre.org/data/csv/699.csv.zip,https://cwe.mitre.org/data/csv/1194.csv.zip,https://cwe.mitre.org/data/csv/1000.csv.zip

# CAPEC: ссылка на CSV-архив каталога
CAPEC_CSV_ZIP_URL=https://capec.mitre.org/data/csv/1000.csv.zip
# CAPEC: ссылка на CSV-архив мэппингов (опционально)
CAPEC_MAPPING_CSV_ZIP_URL=https://capec.mitre.org/data/csv/658.csv.zip

# NVD (фиды JSON по годам)
NVD_FEED_BASE=https://nvd.nist.gov/feeds/json/cve/2.0
# Стартовый год импорта (включительно)
NVD_FROM_YEAR=1999
# Последний год (включительно). Пусто — по текущий год
NVD_TO_YEAR=
# Размер батча при записи в БД
NVD_BATCH=500
# Импортировать дополнительно «modified» фид
NVD_ALSO_MODIFIED=false

# NVD REST API (необязательно, для справки)
NVD_API_URL=https://services.nvd.nist.gov/rest/json/cves/2.0
NVD_API_KEY=
NVD_CACHE_FILE=nvd_cache.json

# CISA KEV (известные эксплуатируемые уязвимости)
CISA_KEV_URL=https://www.cisa.gov/sites/default/files/feeds/known_exploited_vulnerabilities.json

# UI стриминг логов (опционально)
# Размер эмулируемого терминала. Если фронтенд
# присылает columns/rows, они имеют приоритет.
UI_TTY_COLUMNS=100
UI_TTY_ROWS=24
